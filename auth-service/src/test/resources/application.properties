# Test-only configuration, picked up by every auth-service integration test.
#
# A fixed, deterministic base64-encoded 32-byte AES-256 key for the JWKS private-key cipher
# (PrivateKeyCipher). This is NOT a real secret — it exists only so Testcontainers integration tests
# can seal/open signing-key material. Real deployments supply AUTH_JWKS_ENC_KEY from the environment.
auth.jwks.encryption-key=AAECAwQFBgcICQoLDA0ODxAREhMUFRYXGBkaGxwdHh8=
