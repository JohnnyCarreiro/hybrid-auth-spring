/**
 * Cross-service contracts shared by {@code auth-service} and {@code resource-service} (JWT claim
 * names, DTOs both sides agree on). Populated as features need it.
 */
package com.johnnycarreiro.hybridauth.shared;
